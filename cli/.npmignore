# Ignore everything by default
*

# Include only what's needed (also specified in package.json "files")
!dist/
!config/
!README.md
!LICENSE
!package.json

# Explicitly exclude sensitive files (belt and suspenders)
.env*
*.pem
*.key
*secret*
*credential*
node_modules/
src/
tests/
*.test.*
*.spec.*
tsconfig*.json
vitest.config.*
.DS_Store
