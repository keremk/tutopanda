meta:
  name: Script Generation
  description: Generate documentary scripts tailored to the user inquiry.
  id: ScriptProducer
  version: 0.1.0
  author: Tutopanda
  license: MIT

inputs:
  - name: InquiryPrompt
    description: The topic describing the desired movie script.
    type: string
    required: true
  - name: Duration
    description: Desired narration duration in seconds.
    type: int
    required: true
  - name: NumOfSegments
    description: Number of narration segments to produce.
    type: int
    required: true
  - name: Audience
    description: Target audience for tone and vocabulary.
    type: string
    required: false
    default: Adult
  - name: Language
    description: Narration language.
    type: string
    required: false
    default: en

artifacts:
  - name: MovieTitle
    description: The generated title for the documentary.
    type: string
  - name: MovieSummary
    description: Narrative summary for supplemental reading.
    type: string
  - name: NarrationScript
    description: Array of narration paragraphs per segment.
    type: array
    itemType: string
    countInput: NumOfSegments

loops:
  - name: segment
    description: Iterates over narration segments.
    countInput: NumOfSegments

models: 
  - provider: openai
    model: gpt-5-1
    promptFile: ../prompts/script.toml
    inputSchema: ../schemas/openai-input.json
    outputSchema: ../schemas/script-producer.json
    config:
      text_format: json_schema
