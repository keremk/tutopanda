{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",  // Optional: For built JS if you add a build script
    "declaration": true,  // Optional: For .d.ts exports
    "composite": true  // Enables project references (speeds up root builds)
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}